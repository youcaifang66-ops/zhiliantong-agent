name: ci
on:
  push:
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - run: uv sync --frozen --group dev
      - run: uv run ruff check domain infrastructure agent/reliable_memory.py agent/fitness_workflow.py rag/evaluable_retriever.py eval/run_offline_eval.py tests api_app.py
      - run: uv run pytest -q
      - run: uv run python eval/run_offline_eval.py
